/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone', // <--- ADD THIS LINE
  // keep your other settings like images or experimental if they exist
};

export default nextConfig;